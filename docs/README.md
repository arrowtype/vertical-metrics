# Vertical Metrics Testing: Screenshot Process

All macOS screenshots taken on a display scaled to 1920x1080 resolution, with the menu bar cropped out.

InDesign has UI hidden (tab) and is scaled to fit the page to the viewport (Command+2).

TextEdit uses a window manager (Divvy) to make each window take up an even section of the screen.

